# Space Shooter (Python + Pygame)

Juego tipo "space shooter" construido en Python usando Pygame.

## Requisitos

- Python 3.8+ (Windows)
- VS Code (recomendado)
- Extensión “Python” de Microsoft en VS Code

## Instalación y ejecución (Windows + PowerShell en VS Code)

1. Abrir la terminal en la carpeta del proyecto (PowerShell):
   ```powershell
   cd "<ruta a tu carpeta del proyecto>"