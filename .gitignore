# Entornos virtuales .venv/ venv/ env/ # Python: cachés y compilados __pycache__/ *.pyc *.pyo *.pyd # Logs / temporales *.log *.tmp *.bak # Cobertura y pruebas .coverage .coverage.* htmlcov/ .pytest_cache/ .mypy_cache/ # Distribución / empaquetado build/ dist/ *.egg-info/ # IDE / editor .vscode/ .idea/ *.code-workspace # SO .DS_Store Thumbs.db # Variables de entorno locales (si las usas) .env *.env # Otros .sourcemaps/
